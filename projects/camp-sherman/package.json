{
  "name": "camp-sherman-viewer",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node build.mjs",
    "serve": "python3 -m http.server 8086 --bind 127.0.0.1 --directory ../../public/camp-sherman"
  },
  "dependencies": {
    "three": "0.180.0",
    "three-mesh-bvh": "^0.9.14"
  },
  "devDependencies": {
    "@playwright/test": "1.55.1",
    "esbuild": "0.25.10",
    "pngjs": "^7.0.0"
  }
}
